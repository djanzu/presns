# LLM Type: 'openai' or 'ollama'
LLM_TYPE=openai

# OpenAI Settings (Only needed if LLM_TYPE=openai)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# Ollama Settings (Only needed if LLM_TYPE=ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma4:latest
